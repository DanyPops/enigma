{
	"name": "@danypops/enigma",
	"version": "0.1.0",
	"license": "MIT",
	"description": "Encrypted credential vault and supervisor daemon: holds delegated OAuth/API credentials for other daemons, injects them as env into spawned children, never exposes them to anything else",
	"type": "module",
	"files": ["src", "package.json"],
	"bin": {
		"enigma": "src/cli.ts"
	},
	"scripts": {
		"test": "bun test",
		"check": "bun x tsc --noEmit"
	},
	"dependencies": {
		"@danypops/daemon-kit": "^0.3.0",
		"@napi-rs/keyring": "^1.3.0"
	},
	"devDependencies": {
		"@types/node": "^22.19.19",
		"bun-types": "latest",
		"typescript": "^5.9.3"
	},
	"repository": {
		"type": "git",
		"url": "git+https://github.com/DanyPops/enigma.git"
	}
}
