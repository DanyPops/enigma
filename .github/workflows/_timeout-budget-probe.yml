name: timeout-budget-probe

# Disposable: verifies GitHub Actions actually enforces timeout-minutes
# before trusting it as the guardrail for the real ci.yml jobs. Removed
# once confirmed -- not part of the permanent workflow set.
on:
  workflow_dispatch:

jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 1
    steps:
      - run: sleep 180
